{
  "name": "AngularJSPOC",
  "private": true,
  "version": "0.0.0",
  "description": "A sample AngularJS POC",
  "devDependencies": {
    "http-server": "^0.9.0",
    "node-sass": "^4.9.0",
    "npm-run-all": "^4.1.3",
    "sass": "^1.8.0"
  },
  "scripts": {
    "scss-compile": "sass --watch ./app/scss/:./app/css/",
    "server": "http-server -a localhost -p 8000 -o -c-1 ./app",
    "start": "run-p scss-compile server"
  }
}
